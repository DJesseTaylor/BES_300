API with multiple implementations.  Also simulates longer transaction times to demonstrait advantages of async and websockets.

git clone https://github.com/HypertheoryTraining/curbside-client.git has a simple Angualar UI that interacts with the API.
the angular app will require you to update the localhost port to match your API.